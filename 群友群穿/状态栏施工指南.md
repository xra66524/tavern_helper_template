# 《群友群穿》状态栏施工指南

> **本指南为 `MVU脚本施工指南.md` 阶段五的独立展开**。阶段五涉及 15 个 Vue 组件、94 人群像网格、305 节点科技树 DAG 可视化、扁平化机构树、可选 Tab 按数据存在性显隐、机构制度详情弹出面板等大量前端工程，单列一指南便于分步施工与回归测试。
>
> **前提**：阶段一（Schema）、阶段二（initvar）、阶段三（世界书）、阶段四（兜底校验）均已完成。本指南只负责 `群友群穿/界面/状态栏/` 下的前端界面施工。
>
> **参考来源**：
>
> - `MVU脚本施工指南.md` §五（状态栏界面设计草案）与 §六阶段五（核心组件清单）
> - `后期扩展包施工指南.md` §六（状态栏界面扩展：可选 Tab + 弹出面板）
> - `src/2039_ui/主面板/`（2039 政局监测面板，Tab 切换 + 详情弹窗 + 搜索/筛选/排序的完整参考实现）
> - `示例/角色卡示例/界面/状态栏/`（角色卡示例状态栏，TabNav + WorldSection + defineMvuDataStore 的最简参考）
> - `.cursor/rules/前端界面.mdc`（iframe 适配硬性规范）

---

## 一、界面定位与总体架构

### 1.1 叙事定位

USER 是「摄像机/观察者」，状态栏应呈现**群像全景**而非单一角色面板。94 名穿越者 + 305 节点科技树 + 扁平化机构表 + 多势力外部博弈 + 情报网络 + 事件队列，共同构成一个**多 Tab 群像仪表盘**。

### 1.1.1 整体布局方案：手机案卷 + 底部菜单（最终采用）

本状态栏**完全以手机为第一优先**，不再考虑电脑端左侧目录适配。原「横向 Tab + 单内容区」方案改为**手机 APP 式底部菜单 + 案卷正文区**：

```text
┌──────────────────────────────┐
│  QUNYOU CROSSING      LIVE   │
│  📅 1905-05-10  第1天 混乱期 │  ← 顶部世界案卷抬头
│  当前大事件：确认穿越处境     │
├──────────────────────────────┤
│  🚛 基地车  当涂县南岸        │  ← 基地车简报条
│  ⚛️正常  ⚡72%  🦾若干       │
├──────────────────────────────┤
│                              │
│  🔬 案卷 1/7 · 科技           │  ← 当前案卷正文
│  ┌────────────────────────┐  │
│  │ 本页摘要                │  │
│  │ 科技树面板施工中……      │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │ 科技案卷                │  │
│  │ 后续接入 DAG / 进度……   │  │
│  └────────────────────────┘  │
│                              │
├──────────────────────────────┤
│ 🔬科技 🏛组织 ⚔军事 👥角色 🌐外部 │  ← 底部 APP 菜单栏
│ 🕵情报 ⚠事件 💰财政 🗺区划 📋人事 │     横向滚动，可选菜单项按数据存在显示
└──────────────────────────────┘
```

设计原则：

1. **底部菜单即目录**：方案 D 的「左侧目录」在手机上改造为底部固定/粘滞菜单，类似手机 APP 的 Tab Bar。
2. **正文是案卷页**：每个模块不是传统网页 Tab，而是一页「案卷正文」；顶部有案卷编号与摘要卡，后续组件作为案卷内容接入。
3. **不再支持折叠为空**：底部菜单点击即切换案卷，当前案卷始终有正文，避免手机端出现空白折叠态。
4. **手机第一**：宽度、小字号、底部菜单、纵向正文优先；电脑端只保证可用，不单独优化。
5. **叙事风格**：整体视觉从「现代控制台」偏向「1905 年情报案卷 + 基地车电子化抬头」混合风格。

### 1.2 文件结构

```text
群友群穿/界面/状态栏/
├── index.html              # 纯 iframe srcdoc：<head></head><body><div id="app"></div></body>
├── index.ts                # iframe 入口：waitUntil Mvu + stat_data 就绪后挂载 Vue 应用
├── global.css              # 全局 CSS 变量（配色方案、字体）
├── store.ts                # defineMvuDataStore(Schema, {type:'message', message_id:getCurrentMessageId()})
├── App.vue                 # 主布局：WorldSection + BaseVehicleBar + 案卷正文区 + 底部菜单 TabNav
├── components/
│   ├── WorldSection.vue            # 世界状态栏（日期/阶段/大事件/穿越天数 + 当前会议 v-if）
│   ├── BaseVehicleBar.vue          # 基地车状态条（点击弹出详情报文）
│   ├── BaseVehicleDetailModal.vue   # 基地车详细数据弹窗（物资/设备/电能全景）
│   ├── TabNav.vue                  # 手机 APP 式底部菜单栏（核心 7 Tab + 可选 3 Tab v-if）
│   ├── TechTreePanel.vue           # 科技树面板（核心：DAG + 分支进度 + 三档时间预估 + 方向选择）
│   ├── TechNodeCard.vue            # 科技节点详情卡片（前置链/进度条/解锁能力/三档预计完成日期）
│   ├── OrganizationTree.vue        # 组织结构树（按 上级机构 建树 + 三权指针徽标 + 制度图标）
│   ├── InstitutionDetailModal.vue  # 机构制度详情弹出面板（14 字段只读查看）
│   ├── MilitaryPanel.vue           # 军事面板（总兵力/弹药状态条/部队卡片/装备清单）
│   ├── CharacterGrid.vue           # 角色群像网格（核心：94 人 + 非群友，搜索/筛选/排序/分页）
│   ├── CharacterCard.vue           # 单角色卡片（关键标签徽标 + 履历 tooltip）
│   ├── ExternalForcesPanel.vue     # 外部势力面板（态度+外交策略对照表 + 内部分支折叠）
│   ├── IntelligencePanel.vue       # 情报面板（反间谍 + 准确度徽标 + 对外情报网络节点）
│   ├── EventQueue.vue              # 事件队列（近期事件 + 潜在危机）
│   ├── FinancePanel.vue            # 财政面板（v-if 数据存在时显示）
│   ├── AdminDivisionPanel.vue      # 行政区划面板（v-if 数据存在时显示）
│   └── PersonnelPanel.vue          # 人事面板（v-if 数据存在时显示）
```

### 1.3 数据流

```text
stat_data (MVU 变量)
    │
    ▼
store.ts (defineMvuDataStore, 2s 轮询 + watchIgnorable 双向同步)
    │
    ▼
App.vue (useDataStore() → 注入各子组件)
    │
    ├── 静态结构 join：TechTreePanel 读取 tech_tree.ts 静态定义 + MVU 运行时状态
    ├── 派生字段：直接读 store.data.$穿越者存活数 / $已解锁能力清单 / $可启动的科技节点 等
    └── 底部菜单显隐：核心 7 项常驻；财政/区划/人事按数据存在性追加
```

### 1.4 配色与字体（global.css）

参照 `示例/角色卡示例/界面/状态栏/global.css` 的档案机打字风格，但群穿题材偏「末世建国」气质，建议采用**深色仪表盘 + 暖色警示**配色（参照 `src/2039_ui/主面板/App.vue` 的深色面板风格）：

```css
:root {
  /* 主色调：深色仪表盘 */
  --c-bg-deep: #0b1121;        /* 主面板背景 */
  --c-bg-card: #111827;        /* 卡片背景 */
  --c-border: rgba(148, 163, 184, 0.22);
  --c-text: #e2e8f0;           /* 主文字 */
  --c-text-dim: #94a3b8;       /* 次要文字 */

  /* 状态色阶 */
  --c-ok: #22c55e;             /* 正常/充足/稳固 */
  --c-warn: #f59e0b;           /* 紧张/降级/警告 */
  --c-danger: #ef4444;         /* 危急/耗尽/失控 */
  --c-info: #3b82f6;           /* 进行中/主方向 */
  --c-accent: #a855f7;         /* 次方向/特殊高亮 */

  /* 字体 */
  --font-ui: ui-sans-serif, system-ui, -apple-system, 'Segoe UI', 'Microsoft YaHei', sans-serif;
  --font-mono: 'Courier New', 'Microsoft YaHei', monospace;
}
```

> 也可采用浅色档案风格（与示例一致），由 GM 决定。本指南示例代码以深色仪表盘为默认。

---

## 二、入口与骨架

### 2.1 index.html

严格遵循 `前端界面.mdc`：`<head>` 留空（webpack 注入），`<body>` 只放挂载点。

```html
<head></head>
<body>
  <div id="app"></div>
</body>
```

### 2.2 index.ts

参照 `示例/角色卡示例/界面/状态栏/index.ts`：等待 Mvu 框架与 stat_data 就绪后再挂载，避免空数据闪烁。

```ts
import { waitUntil } from 'async-wait-until';
import { createPinia } from 'pinia';
import { createApp } from 'vue';
import App from './App.vue';
import './global.css';

$(async () => {
  await waitGlobalInitialized('Mvu');
  await waitUntil(() => _.has(getVariables({ type: 'message' }), 'stat_data'));
  createApp(App).use(createPinia()).mount('#app');
});
```

### 2.3 store.ts

参照 `示例/角色卡示例/界面/状态栏/store.ts`，直接用 `defineMvuDataStore`：

```ts
import { defineMvuDataStore } from '@util/mvu';
import { Schema } from '../../mvu_schema';

export const useDataStore = defineMvuDataStore(
  Schema,
  { type: 'message', message_id: getCurrentMessageId() },
);
```

> **注意**：`defineMvuDataStore` 内部已实现 2s 轮询 + `watchIgnorable` 双向同步 + `schema.safeParse` 校验回写（见 `util/mvu.ts`）。组件只需 `useDataStore().data` 即可拿到响应式 `stat_data`，无需自行轮询。

### 2.4 App.vue 主布局

采用手机案卷布局：顶部 `WorldSection` + `BaseVehicleBar` 是固定信息抬头；中间为当前案卷正文；底部 `TabNav` 是手机 APP 式菜单栏。需用 `useLocalStorage` 持久化当前案卷，并用横向滚动底部菜单适配窄屏。

```vue
<template>
  <div class="status-panel">
    <WorldSection />
    <BaseVehicleBar />

    <div class="dossier-body">
      <TechTreePanel v-if="active_tab === '科技'" />
      <OrganizationTree v-else-if="active_tab === '组织'" />
      <MilitaryPanel v-else-if="active_tab === '军事'" />
      <CharacterGrid v-else-if="active_tab === '角色'" />
      <ExternalForcesPanel v-else-if="active_tab === '外部'" />
      <IntelligencePanel v-else-if="active_tab === '情报'" />
      <EventQueue v-else-if="active_tab === '事件'" />
      <FinancePanel v-else-if="active_tab === '财政'" />
      <AdminDivisionPanel v-else-if="active_tab === '区划'" />
      <PersonnelPanel v-else-if="active_tab === '人事'" />
    </div>

    <TabNav v-model="active_tab" :tabs="visible_tabs" />
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue';
import { useLocalStorage } from '@vueuse/core';
import { useDataStore } from './store';
import WorldSection from './components/WorldSection.vue';
import BaseVehicleBar from './components/BaseVehicleBar.vue';
import TabNav from './components/TabNav.vue';
import TechTreePanel from './components/TechTreePanel.vue';
import OrganizationTree from './components/OrganizationTree.vue';
import MilitaryPanel from './components/MilitaryPanel.vue';
import CharacterGrid from './components/CharacterGrid.vue';
import ExternalForcesPanel from './components/ExternalForcesPanel.vue';
import IntelligencePanel from './components/IntelligencePanel.vue';
import EventQueue from './components/EventQueue.vue';
import FinancePanel from './components/FinancePanel.vue';
import AdminDivisionPanel from './components/AdminDivisionPanel.vue';
import PersonnelPanel from './components/PersonnelPanel.vue';

const store = useDataStore();

const core_tabs = [
  { id: '科技', label: '科技', icon: '🔬' },
  { id: '组织', label: '组织', icon: '🏛️' },
  { id: '军事', label: '军事', icon: '⚔️' },
  { id: '角色', label: '角色', icon: '👥' },
  { id: '外部', label: '外部', icon: '🌐' },
  { id: '情报', label: '情报', icon: '🕵️' },
  { id: '事件', label: '事件', icon: '⚠️' },
];

const optional_tabs = computed(() => {
  const tabs: { id: string; label: string; icon: string }[] = [];
  if (store.data.组织结构?.财政) tabs.push({ id: '财政', label: '财政', icon: '💰' });
  if (store.data.组织结构?.行政区划) tabs.push({ id: '区划', label: '区划', icon: '🗺️' });
  if (store.data.组织结构?.人事) tabs.push({ id: '人事', label: '人事', icon: '📋' });
  return tabs;
});

const visible_tabs = computed(() => [...core_tabs, ...optional_tabs.value]);

const active_tab = useLocalStorage<string>('群友群穿:status_bar:active_tab', '科技');
</script>

<style lang="scss" scoped>
.status-panel {
  width: 100%;
  box-sizing: border-box;
  padding: 8px;
  color: var(--c-text);
  font-family: var(--font-ui);
  background: var(--c-bg-deep);
  border: 1px solid var(--c-border);
  border-radius: 8px;
}

.dossier-body {
  padding: 14px 12px 86px;
}
</style>
```

> **可选菜单显隐策略**：参照 `后期扩展包施工指南.md` §2.1 的「按数据存在性触发」原则——底部菜单中 `财政/区划/人事` 判断 `store.data.组织结构.财政/行政区划/人事` 是否存在，初期 AI 不填则缺省、菜单项不显示；建政后 AI 按剧情自然补值，菜单项自然出现。**无开关、无锁定、无 schema 切换**。
> **交互约束**：底部菜单点击只切换案卷，不再允许点击当前项折叠为空。手机端应始终显示一个有效案卷页。

---

## 三、世界状态栏与基地车状态条

### 3.1 WorldSection.vue

参照 `示例/角色卡示例/界面/状态栏/components/WorldSection.vue` 的 meta-row + event-list 结构，但字段映射到群穿的「日期/时间/政治阶段/当前大事件/穿越天数/当前会议」。

```vue
<template>
  <div class="world-section">
    <div class="meta-row">
      <span>📅 {{ store.data.世界.当前日期 || '----.--.--' }}</span>
      <span>🕐 {{ store.data.世界.当前时间 || '--:--' }}</span>
      <span>⏳ 穿越第 {{ store.data.世界.穿越天数 }} 天</span>
    </div>
    <div class="phase-row">
      <span class="phase-tag">阶段：{{ store.data.世界.政治阶段 || '--' }}</span>
      <span v-if="store.data.世界.当前会议" class="meeting-tag" :class="{ active: isMeetingActive }">
        🏛️ {{ store.data.世界.当前会议 }}
      </span>
    </div>
    <div class="event-headline">
      <span class="event-label">当前大事件</span>
      <span class="event-text">{{ store.data.世界.当前大事件 || '暂无' }}</span>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue';
import { useDataStore } from '../store';

const store = useDataStore();

const isMeetingActive = computed(() => {
  const m = store.data.世界.当前会议;
  return m && m !== '无';
});
</script>
```

> **当前会议**：`v-if` 字段非空时显示，会议进行中（非「无」）高亮。初期 AI 不填则不显示，建政后自然出现。

### 3.2 BaseVehicleBar.vue（点击触发弹窗）

基地车状态条以图标 + 色阶徽标呈现反应堆/配电/外骨骼概要。

**交互改造**：整条 `vehicle-bar` 增加 `@click` 事件，点击后呼出 `BaseVehicleDetailModal`，展示基地车完整数据。点击区域加 `cursor: pointer` 视觉提示。

#### 使用的字段（从 store.data.基地车）

| 字段             | 显示位置                   |
| ---------------- | -------------------------- |
| `当前位置`       | 条标题左：🚛 当涂县长江南岸 |
| `移动状态`       | 条标题右 · 分隔            |
| `反应堆状态`     | 标签徽标 ⚛️                 |
| `配电系统负载`   | 标签徽标 ⚡%                |
| `外骨骼可用数量` | 标签徽标 🦾                 |

#### 色阶映射（自由字符串 fallback）

```ts
/正常|充足|满功率|驻扎/ → var(--qyc-ok)
/紧张|降|过载|故障/  → var(--qyc-warn)
/危急|损毁|耗尽|失控/ → var(--qyc-danger)
未知取值 → var(--qyc-ink-dim)
```

#### 事件

- `@click` → 父组件通过 `ref` 控制 `BaseVehicleDetailModal` 显隐

色阶映射函数：正常→var(--qyc-ok)、降级/降功率/紧张→var(--qyc-warn)、故障/危急/损毁→var(--qyc-danger)。**注意反应堆状态/配电系统状态等均为自由字符串**，着色时需对未知取值做 fallback（默认色 var(--qyc-ink-dim)），参照 MVU 脚本施工指南的 OrganizationTree 注意事项。

### 3.3 BaseVehicleDetailModal.vue（基地车详细数据弹窗）

点击 `BaseVehicleBar` 后弹出基地车全量数据。采用与 `InstitutionDetailModal` 相同的 Teleport + modal-mask 模式（`position: fixed`），在纸色案卷风格上保持一致。

#### 弹窗布局

```text
┌────────────────────────────────┐
│  🔙  ← 关闭（点击遮罩也可关闭） │
│                                │
│  🚛 基地车档案                   │
│  ──────────────────────         │
│                                │
│  ● 位置与机动                    │
│    当前位置：当涂县长江南岸        │
│    移动状态：驻扎                │
│                                │
│  ● 能源系统                      │
│    反应堆     ⚛️ 正常     满功率 │
│    配电系统   ⚡ 负载 72%  正常   │
│                                │
│  ● 外骨骼                        │
│    可用数量：4                   │
│    备注：磁体库存仅够2次替换       │
│                                │
│  ● 核心设备                      │
│    聚变堆      ✅ 正常           │
│    服务器      ✅ 正常           │
│    数控母机    ✅ 正常           │
│    图形工作站  ⚠️ 降级 显存故障  │
│                                │
│  ● 物资储备                      │
│    给养        🟢 充足           │
│    燃料        🟡 紧张 消耗中    │
│    医药        🟢 充足           │
│    金属备件    🟡 紧张           │
│    现代枪械弹药 🟢 充足           │
│    高产作物种子 🟢 完好           │
│    电子设备    🔴 危急 仅够1月    │
│                                │
└────────────────────────────────┘
```

#### 使用的 Schema 字段

```ts
interface 基地车数据 {
  当前位置: string;
  移动状态: string;
  反应堆功率等级: string;
  配电系统负载: number;
  反应堆状态: string;
  配电系统状态: string;
  物资储备: Record<string, { 储备水平: string; 备注: string }>;
  核心设备状态: Record<string, { 状态: string; 备注: string }>;
  外骨骼可用数量: number;
  外骨骼备注: string;
}
```

#### 分区设计

| 分区       | 内容                                       | 样式                                                      |
| ---------- | ------------------------------------------ | --------------------------------------------------------- |
| 位置与机动 | 当前位置 + 移动状态                        | 两行文本，第二行小字                                      |
| 能源系统   | 反应堆（功率等级+状态）、配电（负载+状态） | 行内标签，每项一行，状态色阶着色                          |
| 外骨骼     | 可用数量 + 备注                            | 数量大字 + 备注灰色小字                                   |
| 核心设备   | 遍历 核心设备状态 record                   | 每项一行：名称 + 状态徽标 + 备注。损毁=红色 ✕             |
| 物资储备   | 遍历 物资储备 record                       | 每项一行：名称 + 储备水平色阶徽标 + 备注。颜色映射同 §3.2 |

#### 关键交互

- `v-if` 控制弹窗显隐（由 BaseVehicleBar 的 @click 事件设置 showVehicleDetail ref）
- Teleport to body + 遮罩关闭
- 纸色卡片风格：`--qyc-paper-light` 背景、`--qyc-vermilion` 分隔线
- 物资列表和核心设备列表用 `Object.entries()` 遍历 z.record
- 储备水平 / 设备状态色阶函数与 §3.2 一致（自由字符串 regex fallback）

#### 组件签名

```vue
<template>
  <Teleport to="body">
    <div class="modal-mask" @click="emit('close')">
      <article class="vehicle-modal" @click.stop>
        <header class="modal-header">
          <div class="modal-title">🚛 基地车档案</div>
          <button type="button" class="close-btn" @click="emit('close')">✕</button>
        </header>

        <!-- 位置与机动 -->
        <section class="detail-section">
          <h3>📍 位置与机动</h3>
          <div class="info-row">
            <span class="info-label">当前位置</span>
            <span class="info-value">{{ v.当前位置 }}</span>
          </div>
          <div class="info-row">
            <span class="info-label">移动状态</span>
            <span class="info-value">{{ v.移动状态 }}</span>
          </div>
        </section>

        <!-- 能源系统 -->
        <section class="detail-section">
          <h3>⚡ 能源系统</h3>
          ...
        </section>

        ...
      </article>
    </div>
  </Teleport>
</template>
```

#### 在 App.vue 中的挂载

```vue
<template>
  <div class="status-panel">
    <article class="dossier-shell">
      <WorldSection />
      <BaseVehicleBar @click="showVehicleDetail = true" />
      <BaseVehicleDetailModal v-if="showVehicleDetail" @close="showVehicleDetail = false" />
      ...
    </article>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue';
const showVehicleDetail = ref(false);
</script>
```

---

## 四、科技树面板（核心组件）

### 4.1 设计要点

科技树是群穿叙事的核心引擎。`TechTreePanel.vue` 需 join 两份数据：

- **静态结构**：`src/群友群穿/tech_tree.ts` 的 `科技树` 常量（305 节点的所属分支/描述/前置科技/研发总耗时_天/解锁能力/备注）
- **运行时状态**：`store.data.科技树.科技节点[节点名]`（已研发天数/启动时间/完成时间）

```ts
import { 科技树 as 科技树定义 } from '../../../tech_tree';
import { useDataStore } from '../store';

const store = useDataStore();

// 节点全貌 = 静态定义 + 运行时状态
const 节点全貌 = computed(() => {
  const 运行时 = store.data.科技树.科技节点 ?? {};
  return _.mapValues(科技树定义, (定义, 节点名) => ({
    ...定义,
    ...(运行时[节点名] ?? { 已研发天数: 0, 启动时间: '', 完成时间: '' }),
  }));
});
```

### 4.2 顶部状态条

- **科技自主度大徽标**：读 `store.data.科技树.科技自主度`，完全依赖=红 / 部分自主=橙 / 基本自主=绿
- **科技发展方针** / **技术保密等级**：文本徽标

### 4.3 研究方向选择区

参照 `MVU脚本施工指南.md` §5.4 的设计：

- **主要研究方向**：下拉框（单选），选项为「可启动的科技节点」（读 `store.data.$可启动的科技节点` 派生字段），每个选项旁显示该方向按 100% 速率的预计完成日期
- **次要研究方向**：多选（最多 3 个），各 30% 速率，旁显示预计完成日期
- **被动研究方向**：只读展示当前已启动但未列入主/次方向的节点（5% 速率）

> **注意**：方向选择是玩家/GM 策略决策，**通过 `watchIgnorable` 双向同步写回 MVU**。玩家在状态栏选择主要研究方向 → `store.data.科技树.主要研究方向` 更新 → `defineMvuDataStore` 自动写回 stat_data → 下一轮兜底脚本规则 11b 按新方向速率累加。

### 4.4 DAG 可视化区

**不引入图库**（避免 iframe 体积膨胀）。按 `前置科技` 字段在内存中重建分层 DAG，用 CSS flex/grid 渲染：

1. **拓扑分层**：Kahn 算法计算每个节点的层级（最长前置链长度），同层节点横向排列
2. **节点着色**：
   - 前置未满足 → 灰显 + 🔒 锁图标
   - 已完成（`完成时间` 非空）→ 绿色边框 + 解锁能力标签
   - 进行中（`启动时间` 非空且 `完成时间` 空）→ 进度条（百分比 = `已研发天数 / 研发总耗时_天 × 100`），进度条颜色按方向区分（主要=金色 `--c-warn` / 次要=蓝色 `--c-info` / 被动=灰色 `--c-text-dim`）
   - 未启动 → 默认色
3. **依赖连线**：用 CSS `::before`/`::after` 伪元素画简易竖线/横线连接父子节点（不追求图形美观，只表达层级关系）；或退化为按分支分组列表 + 缩进表达层级

> **性能**：305 节点全量渲染可能卡顿。建议默认按 `所属分支` 分组折叠，只展开当前主要研究方向所在分支 + 相邻分支；或提供「仅显示已启动 + 可启动」筛选开关。

### 4.5 三档时间预估

每个进行中节点旁显示三档预计完成日期，供玩家做方向决策：

```ts
function 预计完成日期(节点名: string, 方向速率: number): string {
  const 定义 = 科技树定义[节点名];
  const 运行时 = store.data.科技树.科技节点[节点名];
  if (!定义 || !运行时 || 运行时.完成时间) return 运行时?.完成时间 || '';
  const 剩余等效天数 = 定义.研发总耗时_天 - 运行时.已研发天数;
  const 实际天数 = Math.ceil(剩余等效天数 / 方向速率);
  const 启动日 = dayjs(运行时.启动时间 || store.data.世界.当前日期);
  return 启动日.add(实际天数, 'day').format('YYYY-MM-DD');
}
```

- 主：`预计完成日期(节点名, 1.0)`
- 次：`预计完成日期(节点名, 0.3)`
- 被动：`预计完成日期(节点名, 0.05)`

### 4.6 分支进度概览

按 `所属分支` 分组，每分支展示 `已完成节点数 / 总节点数` 进度条。14 个应用分支 + 系统性科研三阶段，共约 17 个分组。

### 4.7 TechNodeCard.vue（节点详情卡片）

点击节点展开（参照 `src/2039_ui/主面板/省厅详情弹窗.vue` 的 Teleport 弹窗模式，或就地展开）：

- 前置科技链（递归展示上游依赖，用缩进表达层级）
- 研发进度条 + 剩余天数
- 产出描述（`描述` 字段）
- 解锁能力标签（`解锁能力` 数组）
- 启动时间 / 完成时间
- 备注（含「依赖基地车」标记的用于科技自主度计算）

---

## 五、组织结构树

### 5.1 扁平化建树

机构表是 `z.record(机构名, 机构Schema)` 的扁平结构，通过 `上级机构` 字段表达层级。在内存中建树：

```ts
function 建机构树(机构表: Record<string, 机构Node>) {
  const 树: Record<string, 机构Node & { 子机构: 机构名[] }> = {};
  // 第一遍：初始化
  for (const [名, 机构] of Object.entries(机构表)) {
    树[名] = { ...机构, 子机构: [] };
  }
  // 第二遍：挂载子机构
  for (const [名, 机构] of Object.entries(树)) {
    if (机构.上级机构 && 树[机构.上级机构]) {
      树[机构.上级机构].子机构.push(名);
    }
  }
  // 顶层机构 = 上级机构为 '' 的节点
  return Object.entries(树).filter(([, n]) => n.上级机构 === '');
}
```

### 5.2 渲染

- 顶部高亮 `当前最高权力机构`（`store.data.组织结构.当前最高权力机构`）
- 已撤销机构（`运作状态 === '已撤销'`）灰显
- 每个机构节点展示：一把手 / 二把手 / 运作状态 / 权势指数 / 实有人数
- `制度备注` 非空时折叠展示
- **运作状态着色 fallback**：`运作状态` 为自由字符串，未知取值用默认色（参照 `MVU脚本施工指南.md` §5.4 注意事项）

### 5.3 三权指针徽标（v-if）

树顶追加三权指针徽标，`v-if` 字段存在时显示：

- `当前决策核心` → 决策机构名（可点击跳转至该机构节点）
- `当前执行首脑` → 行政执行机构名
- `当前监督机构` → 监察机构名

### 5.4 制度图标 → InstitutionDetailModal

机构节点标题旁追加「⚙」图标，点击 → 弹出 `InstitutionDetailModal.vue`，展示该机构的 `制度详情` 子对象全部 14 个字段。若该机构尚无 `制度详情`（初期旧机构），弹窗内字段为空。

### 5.5 编制数/实有人数对比（v-if）

机构节点列表中追加 `编制数 / 实有人数` 对比，`v-if` `制度详情.编制数` 存在时显示，超编（实有人数 > 编制数）红色。

### 5.6 InstitutionDetailModal.vue

参照 `src/2039_ui/主面板/省厅详情弹窗.vue` 的 Teleport + modal-mask 模式：

```vue
<template>
  <Teleport to="body">
    <div class="modal-mask" @click="emit('close')">
      <article class="institution-modal" @click.stop>
        <header class="modal-header">
          <div class="modal-title">🏛️ {{ 机构名 }} · 制度详情</div>
          <button type="button" class="close-btn" @click="emit('close')">✕</button>
        </header>
        <section class="detail-grid">
          <div v-for="field in 制度字段" :key="field.key" class="detail-item">
            <span class="detail-label">{{ field.label }}</span>
            <span class="detail-value">{{ 制度详情?.[field.key] || '—' }}</span>
          </div>
        </section>
      </article>
    </div>
  </Teleport>
</template>
```

14 个字段：所属体系 / 机构性质 / 级别 / 职务体系 / 领导体制 / 上级业务主管 / 决策机制 / 议事规则 / 任命权限 / 职级 / 编制数 / 依托部门 / 组长级别 / 请示报告要求 / 保密等级。

**只读查看**：AI 更新走 MVU 常规流程，弹出面板不提供直接编辑（与 `后期扩展包施工指南.md` §6.4 一致）。弹出面板用 `position: fixed` + 遮罩实现，点击遮罩或关闭按钮关闭。

---

## 六、军事面板

参照 `MVU脚本施工指南.md` §5.4 的 MilitaryPanel 设计：

- **顶部状态条**：总兵力 + 现代弹药储备（色阶警示：充足→绿 / 紧张→橙 / 耗尽→红）
- **部队卡片**：按 `军事.部队` record 分卡片，每卡展示兵力 / 训练水平 / 士气（表情图标 😠😐😊）/ 当前位置 / 当前任务
- **武器装备清单**：表格展示数量 / 来源 / 状态

- **弹药储备='耗尽' 时顶部红色警示**

---

## 七、角色群像网格（核心组件）

### 7.1 设计要点

94 人群像是本卡的核心展示。`CharacterGrid.vue` 需支持搜索 / 筛选 / 排序 / 分页，避免一次性渲染 94+ 卡片撑高父容器。

### 7.2 双 Tab 结构

分两个子 Tab：「群友」（94 人固定，读 `store.data.群友角色`）与「非群友」（动态增长，读 `store.data.非群友角色`）。

### 7.3 搜索/筛选/排序

参照 `src/2039_ui/主面板/省厅面板.vue` 的 search-bar + filter-bar + sort-group 结构：

- **搜索框**：按姓名 / 履历 / 关键标签模糊匹配
- **筛选**：
  - 原群归属（读世界书档案的静态属性，需在 initvar 中预填或运行时从 `群友角色[名].原群归属` 读取）
  - 职务（实时从机构表推导：遍历 `store.data.组织结构.机构`，收集 `一把手/二把手/副职/成员` 中含该角色的机构名）
  - 安全状态（`人身安全` 字段：健康/轻伤/重伤/已死亡）
  - 关键标签（`关键标签` 数组：已死亡/叛变/重伤等）
- **排序**：姓名 / 安全状态 / 卷入度 / 出场次数

### 7.4 所属系统推导

按所属系统筛选时需实时从机构表推导（沿 `上级机构` 链取顶层机构名）：

```ts
function 推导顶层机构(角色名: string, 机构表: Record<string, 机构Node>): string {
  for (const [机构名, 机构] of Object.entries(机构表)) {
    if ([机构.一把手, 机构.二把手, ...机构.副职, ...机构.成员].includes(角色名)) {
      // 沿 上级机构 链上溯至顶层
      let 当前 = 机构名;
      while (机构表[当前]?.上级机构) 当前 = 机构表[当前].上级机构;
      return 当前;
    }
  }
  return '无所属';
}
```

### 7.5 CharacterCard.vue

每个角色卡片展示：

- 姓名 + 性别 + 年龄
- 关键标签徽标（已死亡=红 / 叛变=紫 / 重伤=橙 等）
- 当前职务（实时推导）
- 人身安全色阶
- 履历：悬浮 tooltip 或展开行展示最近 5 条（`履历` 数组，`_.takeRight(履历, 5)`）

### 7.6 分页/虚拟滚动

**94 人网格需虚拟滚动或分页，避免撑高父容器**（`MVU脚本施工指南.md` §5.5）。建议简单分页：每页 24 人（6×4 网格），底部分页器。虚拟滚动可后续优化。

---

## 八、外部势力 / 情报 / 事件面板

### 8.1 ExternalForcesPanel.vue

- 表格/卡片对比各势力的态度 + 了解程度 + 我方外交策略
- `内部分支` 非空时折叠展示可拉拢人物
- 微观本地势力（士绅/宗族/会党等）与宏观势力同表展示，可按 `在当地影响力` 或键名前缀分组

### 8.2 IntelligencePanel.vue

- 反间谍措施与效果徽标
- 情报准确度大徽标（绿/黄/橙/红对应高/中/低/盲区，读 `store.data.$情报准确度` 或综合评估）
- 对外情报网络以节点卡片展示（目标势力 → 来源 → 可靠性 → 最近情报）
- 可靠性='不可靠' 或 '反间嫌疑' 时卡片边框警示色

### 8.3 EventQueue.vue

- 近期已发生事件列表（`事件队列.近期已发生事件`）
- 潜在危机列表（`事件队列.潜在危机`），按危机等级排序，高危机红色

---

## 九、可选 Tab 面板（财政 / 区划 / 人事）

参照 `后期扩展包施工指南.md` §6.2 的设计，三个面板均 `v-if` 数据存在时显示。

### 9.1 FinancePanel.vue

- 收入来源 / 主要支出柱状图（用 CSS bar 或简单 `div` 比例条）
- `预算审批权` / `审计机构` 文本
- `财政状况` 徽标（赤字/破产用警示色）
- `货币单位` / `金银比价` 文本

### 9.2 AdminDivisionPanel.vue

- 按 `层级` 字段分组展示区划
- 每项标注治理机构 / 人口 / 治安 / 与中央关系
- `阳奉阴违` / `对抗` 用警示色

### 9.3 PersonnelPanel.vue

- `干部管理原则` / `职级体系` 文本
- 近期 `关键岗位任命记录` 时间线列表（`_.takeRight(记录, 10)`）

---

## 十、iframe 适配硬性规范

遵循 `.cursor/rules/前端界面.mdc`：

1. **禁用 `vh`**：高度调整用 `width` + `aspect-ratio`，或让内容自然撑高（但需避免 `min-height` / `overflow: auto` 强制撑高父容器）
2. **94 人网格需虚拟滚动或分页**，避免撑高父容器
3. **优先 tailwindcss**，复杂样式用 `<style lang="scss" scoped>`
4. **页面必须有外部支撑**，主体内容不能用 `position: absolute` 脱离文档流（`InstitutionDetailModal` 的 `position: fixed` 是例外，因为它用 Teleport 挂载到 body 且有遮罩）
5. **页面整体应适配容器宽度，不产生横向滚动条**；TabNav 在窄屏需横向滚动（`overflow-x: auto` + 隐藏滚动条）
6. **`<img>` 标签禁止 `src=""` 占位**，会导致 webpack 打包错误

---

## 十一、挂载到角色卡

### 11.1 第一条消息占位符

参照 `2039/第一条消息/0.txt` 末尾的 `<StatusPlaceHolderImpl/>`，在 `群友群穿/第一条消息/0.txt` 末尾追加状态栏占位符：

```html
<StatusPlaceHolderImpl/>
```

### 11.2 正则替换

参照 `2039/2039.yaml` 的「状态栏美化」正则，在 `群友群穿/群友群穿.yaml` 的 `正则` 段追加：

```yaml
- 正则名称: 状态栏美化
  id: <生成新 UUID>
  启用: true
  查找表达式: <StatusPlaceHolderImpl/>
  内容: |-
    <body>
      <script>
        $('body').load('http://localhost:5500/dist/界面/状态栏/index.html')
      </script>
    </body>
  来源:
    用户输入: false
    AI输出: true
  作用于:
    仅格式显示: true
    仅格式提示词: false
```

> **注意**：正则 `内容` 字段直接内嵌 `<body>` + `$('body').load(...)`，由酒馆助手将 `<StatusPlaceHolderImpl/>` 替换为加载本地 webpack dev server 输出的 iframe。生产环境（推送至酒馆）时路径改为 `dist/界面/状态栏/index.html` 的实际托管地址。

### 11.3 webpack 入口自动识别

`webpack.config.ts` 第 54 行的 `fs.globSync('{示例,src,群友群穿}/**/index.{ts,tsx,js,jsx}')` 会自动识别 `群友群穿/界面/状态栏/index.ts` 作为入口，无需手动配置。由于输出规则会剥离入口路径的第一段目录，构建后输出至 `dist/界面/状态栏/index.html`。

---

## 十二、施工步骤（推荐顺序，约 4-6 天）

### 阶段 5.1：骨架与入口（0.5 天）

1. [x] 创建 `群友群穿/界面/状态栏/` 目录
2. [ ] 创建 `index.html`（纯 `<div id="app"></div>`）
3. [ ] 创建 `index.ts`（waitUntil Mvu + stat_data 就绪后挂载）
4. [ ] 创建 `global.css`（CSS 变量定义）
5. [ ] 创建 `store.ts`（defineMvuDataStore）
6. [ ] 创建 `App.vue` 骨架（WorldSection + BaseVehicleBar + TabNav + Tab 内容区占位）
7. [ ] 创建 `components/TabNav.vue`（参照示例的 TabNav.vue）
8. [x] 启动 `pnpm build`，确认 `dist/界面/状态栏/index.html` 生成
9. [x] 在 `群友群穿/第一条消息/0.txt` 末尾追加 `<StatusPlaceHolderImpl/>`
10. [x] 在 `群友群穿/群友群穿.yaml` 追加「状态栏美化」正则
11. [ ] 推送角色卡到酒馆，确认空状态栏能加载（此时 Tab 内容区为空）

### 阶段 5.2：世界状态栏 + 基地车状态条 + 基地车详情报文（0.5 天）

1. [ ] 实现 `WorldSection.vue`（日期/时间/政治阶段/当前大事件/穿越天数 + 当前会议 v-if）
2. [ ] 实现 `BaseVehicleBar.vue`（反应堆/配电/外骨骼标签 + 色阶 fallback + @click 触发弹窗）
3. [ ] 实现 `BaseVehicleDetailModal.vue`（Teleport + modal-mask + 位置/能源/外骨骼/核心设备/物资储备五分区）
4. [ ] 在 App.vue 中挂载弹窗：ref 控制显隐 + v-if 渲染
5. [ ] 测试：点击基地车条弹窗正常显示，物资/设备列表遍历正确，色阶 fallback 覆盖未知取值

### 阶段 5.3：组织结构树 + 制度详情弹窗（1 天）

1. [ ] 实现 `OrganizationTree.vue`（扁平化建树 + 顶层机构高亮 + 已撤销灰显 + 运作状态着色 fallback）
2. [ ] 追加三权指针徽标（v-if 当前决策核心/执行首脑/监督机构 存在时显示）
3. [ ] 追加编制数/实有人数对比（v-if 制度详情.编制数 存在时显示，超编红色）
4. [ ] 追加机构节点「⚙」图标 → 弹出 `InstitutionDetailModal.vue`
5. [ ] 实现 `InstitutionDetailModal.vue`（Teleport + modal-mask + 14 字段只读展示）
6. [ ] 测试：点击「⚙」弹窗正常显示，点击遮罩/关闭按钮正常关闭

### 阶段 5.4：科技树面板（核心，1.5 天）

1. [ ] 实现 `TechTreePanel.vue` 顶部状态条（科技自主度大徽标 + 方针/保密/吸收能力）
2. [ ] 实现研究方向选择区（主要方向下拉 + 次要方向多选 + 预计完成日期）
3. [ ] 实现拓扑分层算法（Kahn 算法计算层级）
4. [ ] 实现 DAG 可视化区（按分支分组折叠 + 节点着色 + 进度条）
5. [ ] 实现三档时间预估函数
6. [ ] 实现分支进度概览（14 应用分支 + 系统性科研三阶段）
7. [ ] 实现 `TechNodeCard.vue`（前置链/进度条/解锁能力/三档预计完成日期）
8. [ ] 性能测试：305 节点全量渲染是否卡顿，必要时加「仅显示已启动 + 可启动」筛选

### 阶段 5.5：角色群像网格（核心，1 天）

1. [ ] 实现 `CharacterGrid.vue` 双 Tab（群友 / 非群友）
2. [ ] 实现搜索框 + 筛选栏（原群归属/职务/安全状态/关键标签）+ 排序下拉
3. [ ] 实现 `CharacterCard.vue`（姓名/性别/年龄 + 关键标签徽标 + 当前职务 + 人身安全色阶 + 履历 tooltip）
4. [ ] 实现所属系统推导函数（沿 上级机构 链上溯至顶层）
5. [ ] 实现分页（每页 24 人）
6. [ ] 测试：94 人网格分页正常，搜索/筛选/排序生效

### 阶段 5.6：军事 / 外部 / 情报 / 事件面板（1 天）

1. [ ] 实现 `MilitaryPanel.vue`（总兵力/弹药状态条 + 部队卡片 + 装备清单）
2. [ ] 实现 `ExternalForcesPanel.vue`（态度+外交策略对照表 + 内部分支折叠）
3. [ ] 实现 `IntelligencePanel.vue`（反间谍 + 准确度徽标 + 对外情报网络节点）
4. [ ] 实现 `EventQueue.vue`（近期事件 + 潜在危机列表）

### 阶段 5.7：可选 Tab 面板（0.5 天）

1. [ ] 实现 `FinancePanel.vue`（v-if 财政 存在时显示）
2. [ ] 实现 `AdminDivisionPanel.vue`（v-if 行政区划 存在时显示）
3. [ ] 实现 `PersonnelPanel.vue`（v-if 人事 存在时显示）
4. [ ] 测试：初期 AI 不填这些字段时 Tab 不显示；手工补值后 Tab 自然出现

### 阶段 5.8：iframe 适配测试与回归（0.5 天）

1. [ ] 确认无横向滚动条
2. [ ] 确认高度自适应（禁用 vh，94 人网格分页正常）
3. [ ] 确认 `InstitutionDetailModal` 的 `position: fixed` + 遮罩正常工作
4. [ ] 确认 TabNav 窄屏横向滚动
5. [ ] 推送角色卡到酒馆，实际对话测试状态栏随 stat_data 变化更新

---

## 十三、关键注意事项

### 13.1 数据流单向原则

- 状态栏**只读** `store.data`，除「研究方向选择」等少数玩家/GM 策略决策字段外，**不直接编辑 stat_data**
- 玩家策略决策（如主要研究方向）通过 `defineMvuDataStore` 的 `watchIgnorable` 双向同步写回，由兜底脚本在下一轮 `VARIABLE_UPDATE_ENDED` 时按新值累加
- AI 的叙事更新走 MVU 常规流程（`变量更新规则.yaml` + 兜底校验），状态栏只负责展示

### 13.2 自由字符串着色 fallback

`运作状态` / `反应堆状态` / `人身安全` / `财政状况` 等均为自由字符串（AI 可按剧情扩展），着色时必须对未知取值做 fallback：

```ts
function 状态色阶(值: string, 映射: Record<string, string>): string {
  return 映射[值] ?? 'var(--c-text-dim)'; // 未知取值用默认色
}
```

### 13.3 静态结构 join

科技树面板必须 join `tech_tree.ts` 静态定义 + MVU 运行时状态，**不能只读 MVU**（MVU 只存已研发天数/启动时间/完成时间，不含前置依赖/研发总耗时/解锁能力）。新增节点流程：先改 `科技树DAG指南.md` → 同步改 `tech_tree.ts` → 无需改状态栏。

### 13.4 性能

- 305 节点科技树：默认按分支折叠，只展开当前主要方向所在分支
- 94 人群像：分页（每页 24 人）或虚拟滚动
- `defineMvuDataStore` 已内置 2s 轮询 + `_.isEqual` 去重，组件无需自行轮询
- 避免在 `computed` 中做重计算（如拓扑分层），可用 `watchEffect` + `ref` 缓存

### 13.5 与后期扩展包的边界

本指南已合并 `后期扩展包施工指南.md` §6 的全部状态栏扩展内容（`InstitutionDetailModal` / `FinancePanel` / `AdminDivisionPanel` / `PersonnelPanel` / 三权指针 / 当前会议）。**后期扩展包施工指南已完成其使命**，后续状态栏施工以本指南为准。

---

## 十四、参考文件索引

| 用途                                       | 文件路径                                                  |
| ------------------------------------------ | --------------------------------------------------------- |
| 前端界面规则                               | `.cursor/rules/前端界面.mdc`                              |
| MVU 数据 store 工具                        | `util/mvu.ts`（`defineMvuDataStore`）                     |
| TabNav + WorldSection 最简参考             | `示例/角色卡示例/界面/状态栏/`                            |
| 深色仪表盘 + 详情弹窗 + 搜索/筛选/排序参考 | `src/2039_ui/主面板/`                                     |
| 科技树静态定义                             | `src/群友群穿/tech_tree.ts`                               |
| 本角色卡 Schema                            | `src/群友群穿/mvu_schema.ts`                              |
| 本角色卡兜底校验                           | `src/群友群穿/脚本/兜底校验/index.ts`                     |
| 状态栏设计草案                             | `MVU脚本施工指南.md` §五、§六阶段五                       |
| 状态栏扩展设计                             | `后期扩展包施工指南.md` §六                               |
| webpack 入口自动识别                       | `webpack.config.ts` 第 54 行 `fs.globSync`                |
| 2039 状态栏挂载参考                        | `2039/第一条消息/0.txt` + `2039/2039.yaml` 状态栏美化正则 |
