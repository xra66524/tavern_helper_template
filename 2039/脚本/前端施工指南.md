# 2039 前端界面 —— 施工指南

## 一、概述

本指南规划 2039 角色卡所需的所有前端界面，包括**状态栏**、**数据仪表盘**、**流式楼层增强**三大类。目标是让玩家在酒馆中**一眼看懂当前政局状态**，同时为"模拟政府执政"的叙事提供沉浸式的视觉反馈。

### 界面全景

```
酒馆界面
├── 聊天区（流式楼层界面）
│   └── [扩展] 消息楼层增强：高亮变量信息、分段折叠、危机预警标签
│
├── 消息楼层前端界面（单个 iframe 主面板）
│   └── [新增] 综合主面板
│       ├── 状态栏：政治阶段 + 首相状态 + 危机等级（常驻顶部）
│       └── Tab 切换详情区
│           ├── [Tab] 内阁面板：阁僚双线追踪
│           ├── [Tab] 政策追踪器：当前政策生命周期看板
│           ├── [Tab] 选举看板：候选人排名 + 选举热度
│           └── [Tab] 危机仪表盘：四维雷达 + 潜在危机
│
├── [后期] 脚本悬浮窗口（脱离消息楼层，全局常驻）
│   └── 衍生内容处理（阁僚详情弹窗、政策详情抽屉、事件通知等）
│
└── 变量数据层（Pinia store）
    └── 2秒轮询同步 stat_data → 主面板自动更新
```

### 核心原则

1. **变量驱动 UI**——所有界面数据来源于 `stat_data`，通过 Pinia store 的2秒轮询自动刷新，不需要手动触发
2. **一眼读懂政局**——状态栏必须紧凑、直观、即时可读，用颜色/图标辅助
3. **渐进式施工**——先做主面板骨架（状态栏 + Tab 容器），再逐个实现各 Tab 详情面板，最后做悬浮窗口
4. **单 iframe 聚合**——所有核心面板整合到同一个 iframe 主面板中，共享一个 Pinia store，避免多 iframe 的数据同步问题
5. **脚本补位**——脱离消息楼层的全局功能（悬浮窗口）用脚本 + jQuery 实现，不依赖 iframe
6. **适配卡片形状**——前端界面在消息楼层中显示，天然适合卡片比例
7. **移动优先**——所有主面板组件先按 320px～430px 宽度设计，再向桌面端扩展；禁止依赖横向并排布局作为唯一显示方式

### 涉及文件

| 文件                                | 用途                               |
| ----------------------------------- | ---------------------------------- |
| `src/2039_ui/主面板/index.ts`       | 主面板入口，挂载 Vue 应用          |
| `src/2039_ui/主面板/index.html`     | 主面板静态骨架                     |
| `src/2039_ui/主面板/App.vue`        | 主面板容器：状态栏 + Tab 切换      |
| `src/2039_ui/主面板/store.ts`       | 唯一 Pinia store，绑定 `stat_data` |
| `src/2039_ui/主面板/状态栏.vue`     | 常驻顶部的紧凑信息条               |
| `src/2039_ui/主面板/内阁面板.vue`   | Tab: 阁僚卡片网格                  |
| `src/2039_ui/主面板/政策追踪.vue`   | Tab: 政策时间线                    |
| `src/2039_ui/主面板/选举看板.vue`   | Tab: 候选人排名 + 模式切换         |
| `src/2039_ui/主面板/危机仪表盘.vue` | Tab: 四维雷达 + 潜在危机           |
| `src/2039_ui/主面板/阁僚卡片.vue`   | 子组件                             |
| `src/2039_ui/主面板/政策条目.vue`   | 子组件                             |
| `src/2039_ui/主面板/详情弹窗.vue`   | 通用模态框 / 抽屉组件              |
| `src/2039_ui/流式增强/index.ts`     | 流式楼层增强入口                   |
| `src/2039_ui/流式增强/App.vue`      | 变量高亮 + 分段折叠                |
| `src/2039_ui/流式增强/store.ts`     | 流式消息上下文 store               |
| `src/2039_悬浮窗口/index.ts`        | [后期] 悬浮窗口脚本入口            |

---

## 二、技术栈与模式

### 2.1 技术栈

| 技术                    | 用途     | 备注                                |
| ----------------------- | -------- | ----------------------------------- |
| Vue 3 (Composition API) | 组件框架 | 通过 `createApp` 挂载               |
| Pinia                   | 状态管理 | `defineMvuDataStore` 封装了变量轮询 |
| TypeScript              | 类型安全 | 导入 `Schema` 类型                  |
| SCSS (scoped)           | 样式     | 禁止用 `<link>` 引用外部 CSS        |
| Tailwind CSS            | 工具类   | 仅 iframe 宿主可用                  |
| FontAwesome             | 图标     | 免费图标集可用                      |

### 2.2 Pinia Store 模式

每个前端界面必须建立自己的 Pinia store，通过 `defineMvuDataStore` 绑定当前楼层的 MVU 数据：

```ts
// store.ts
import { defineMvuDataStore } from '@util/mvu';
import type { Schema } from '../../2039_mvu/mvu_schema'; // 仅导入类型

export const useDataStore = defineMvuDataStore(
  Schema,  // 传入 Schema（仅用于类型推断和 safeParse）
  { type: 'message', message_id: getCurrentMessageId() }
);
```

**关键特性**：

- `useDataStore().data` 是一个 Vue `Ref<MvuData>`，每2秒自动同步
- Schema 的 `.transform(deriveAll)` 在每次同步时自动执行，派生字段（`$执政合法性` 等）始终最新
- `ignoreUpdates` 机制保证 UI 修改不会触发死循环

### 2.3 入口文件骨架

**index.ts**：创建 Vue 应用并挂载

```ts
import { createApp } from 'vue';
import App from './App.vue';

$(() => {
  createApp(App).mount('#app');
});
```

**index.html**：静态骨架

```html
<head></head>
<body>
  <div id="app"></div>
</body>
```

### 2.4 iframe 宿主选择

| 宿主类型        | 样式隔离   | Tailwind | 酒馆样式继承 | 适用场景                     |
| --------------- | ---------- | -------- | ------------ | ---------------------------- |
| `iframe` (默认) | ✅ 完全隔离 | ✅ 可用   | ❌ 不继承     | 复杂独立界面（仪表盘、面板） |
| `div`           | ❌ 不隔离   | ❌ 禁止   | ✅ 继承       | 需融入酒馆样式的轻量组件     |

> **推荐**：2039 所有面板界面使用 `iframe` 宿主（样式隔离 + Tailwind 可用）

### 2.5 主面板容器架构（推荐）

为了避免多 iframe 的数据同步问题，**所有核心面板整合到同一个 iframe 主面板中**，通过 Tab 切换展示不同内容。

```
┌──────────────────────────────────────────────────┐
│  主面板（单个 iframe，跟随消息楼层）                │
│  ┌────────────────────────────────────────────┐  │
│  │  状态栏（常驻顶部，紧凑信息条）              │  │
│  │  🔴 看守停摆期 │ 首相：空位 │ ⚠️ 危机：严重 │  │
│  ├───────── Tab 栏 ──────────────────────────┤  │
│  │  [内阁] [政策] [选举] [危机]              │  │
│  ├────────────────────────────────────────────┤  │
│  │                                            │  │
│  │  当前 Tab 的内容区域                        │  │
│  │  （点击 Tab 切换，再点同一 Tab 折叠）        │  │
│  │                                            │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

**架构优势**：

| 方案                                 | 优点                                      | 缺点                                                |
| ------------------------------------ | ----------------------------------------- | --------------------------------------------------- |
| ✅ **单 iframe + Tab 切换（本方案）** | 共享一个 store，数据一致；纯 Vue 实现简单 | 单个 iframe 体积略大                                |
| ❌ 多个独立 iframe                    | 职责分离清晰                              | 各自独立 store 不同步；浪费内存和连接；状态同步困难 |

**状态栏与 Tab 区域的交互**：

- 默认：只显示状态栏（最小高度），Tab 区域折叠
- 点击 Tab 标签 → 展开 Tab 区域，显示对应内容
- 点击已激活的 Tab → 折叠 Tab 区域，回到仅状态栏模式
- 这样既"一眼看懂"当前政局，又不会每条消息都占很大空间

---

## 三、界面详细设计

### 3.0 主面板容器（优先级：🔴 最高，贯穿全流程）

**定位**：所有核心面板的载体。一个 iframe 内包含常驻状态栏 + Tab 切换详情区，所有数据共享同一个 Pinia store。

**App.vue 伪代码结构**：

```vue
<template>
  <div class="main-panel">
    <!-- 状态栏：始终显示在顶部 -->
    <StatusBar />

    <!-- Tab 栏：点击切换详情面板 -->
    <div class="tab-bar">
      <button v-for="tab in tabs" :key="tab.key"
        :class="{ active: currentTab === tab.key }"
        @click="toggleTab(tab.key)">
        {{ tab.label }}
      </button>
    </div>

    <!-- Tab 内容区：仅当展开时显示 -->
    <Transition name="slide">
      <div v-show="expanded" class="tab-content">
        <内阁面板 v-if="currentTab === 'cabinet'" />
        <政策追踪 v-if="currentTab === 'policy'" />
        <选举看板 v-if="currentTab === 'election'" />
        <危机仪表盘 v-if="currentTab === 'crisis'" />
      </div>
    </Transition>
  </div>
</template>
```

**核心状态**：

```ts
const expanded = ref(false);
const currentTab = ref<'cabinet' | 'policy' | 'election' | 'crisis'>('cabinet');
const toggleTab = (tab: string) => {
  if (currentTab.value === tab) expanded.value = !expanded.value;
  else { currentTab.value = tab; expanded.value = true; }
};
```

**数据流**：所有 Tab 组件通过 `useDataStore()` 读取同一份 `data`，任何派生字段（`$执政合法性` 等）在所有 Tab 中保持一致。

**移动端适配**：

- Tab 栏使用 `display: flex; overflow-x: auto` 支持横向滚动，按钮最小触摸区域不小于 `44px`
- Tab 内容区在手机端强制单列布局，不使用横向并排
- 详情弹窗在手机端改为底部抽屉（见 3.0 详情弹窗移动端规则）
- 使用 CSS Container Query（`container-type: inline-size`）根据容器宽度切换布局，而非依赖 `window.innerWidth`（因为主面板在 iframe 内）

---

### 3.1 状态栏（优先级：🔴 最高，第1轮施工）

**定位**：常驻显示的最紧凑信息条，让玩家一眼看懂"现在日本处于什么状态"。

**显示内容**：

```
┌──────────────────────────────────────────────┐
│ 🔴 看守停摆期  │  首相：空位  │  ⚠️ 危机：严重 │
│ 内阁稳定度 ████░░░░░░ 35  │  外部压力：高    │
└──────────────────────────────────────────────┘
```

| 字段       | 数据来源                           | 显示方式                 |
| ---------- | ---------------------------------- | ------------------------ |
| 政治阶段   | `$当前剧情焦点` 或 `世界.政治阶段` | 彩色标签（7色对应7阶段） |
| 首相状态   | `日本执政.首相状态` + `.首相姓名`  | 图标 + 文字              |
| 内阁稳定度 | `日本执政.本届内阁.内阁稳定度`     | 进度条（0-100）          |
| 危机等级   | `$整体危机等级`                    | 5级颜色标签              |
| 外部压力   | `$外部压力指数`                    | 4级图标                  |
| 执政合法性 | `$执政合法性`                      | 4级标签                  |
| 政府控制力 | `$政府控制力`                      | 5级进度条                |

**政治阶段颜色映射**：

| 政治阶段       | 颜色         | 含义     |
| -------------- | ------------ | -------- |
| 遇刺潮末期     | `#ef4444` 红 | 危险     |
| 看守停摆期     | `#f97316` 橙 | 紧急     |
| 正常选举期     | `#eab308` 黄 | 过渡     |
| 特别大选报名期 | `#22c55e` 绿 | 机会     |
| 特别大选初选期 | `#3b82f6` 蓝 | 进行中   |
| 特别大选指名期 | `#8b5cf6` 紫 | 关键节点 |
| 新内阁执政期   | `#06b6d4` 青 | 稳定     |

**危机等级颜色映射**：

| 危机等级 | 颜色           | 图标 |
| -------- | -------------- | ---- |
| 正常     | `#22c55e` 绿   | ✅    |
| 注意     | `#eab308` 黄   | ℹ️    |
| 警戒     | `#f97316` 橙   | ⚠️    |
| 严重     | `#ef4444` 红   | 🔴    |
| 危急     | `#7f1d1d` 深红 | 💀    |

#### App.vue 伪代码结构

```vue
<template>
  <div class="status-bar">
    <div class="status-row-1">
      <PhaseTag :phase="data.$当前剧情焦点" :stage="data.世界?.政治阶段" />
      <PrimeMinister :status="data.日本执政?.首相状态" :name="data.日本执政?.首相姓名" />
      <CrisisLevel :level="data.$整体危机等级" />
    </div>
    <div class="status-row-2">
      <ProgressBar label="内阁稳定度" :value="data.日本执政?.本届内阁?.内阁稳定度 ?? 0" />
      <PressureIcon :level="data.$外部压力指数" />
      <LegitimacyTag :level="data.$执政合法性" />
    </div>
  </div>
</template>

<script setup lang="ts">
import { useDataStore } from './store';
const { data } = storeToRefs(useDataStore());
</script>
```

#### 移动端适配

手机端状态栏空间有限，默认只显示 3 项核心信息，其余折叠：

| 信息       | 桌面端             | 手机端                         |
| ---------- | ------------------ | ------------------------------ |
| 政治阶段   | 完整标签           | 完整标签（最高优先级）         |
| 首相状态   | 图标 + 姓名 + 状态 | 简写："首相：空位/姓名"        |
| 危机等级   | 图标 + 文本        | 图标 + 文本                    |
| 内阁稳定度 | 进度条             | 收进展开区，显示为小型数字标签 |
| 外部压力   | 标签               | 收进展开区                     |
| 执政合法性 | 标签               | 收进展开区                     |
| 政府控制力 | 进度条             | 收进展开区                     |

手机端默认显示：

```
🔴 看守停摆期  首相：空位  危机：严重
```

点击后展开第二行：

```
稳定度 35  外压 高  合法性 低  控制力 42
```

---

### 3.2 内阁面板（优先级：🔴 高，第2轮施工）

**定位**：以卡片网格展示所有阁僚，每张卡片追踪"双线"——政治能力（上） + 少女视角（下）。

**卡片布局**：

```
┌──────────────────────┐
│ 🏛️ 外务大臣           │  ← 职位名
│ 丰川祥子  16岁        │  ← 姓名 年龄
│ ───────────────────  │
│ 理解度：完全不懂       │  ← 政治能力线
│ 履职：恐慌中           │
│ 操控者：事务次官会议   │
│ 操控程度 ████████░░ 85│
│ ───────────────────  │
│ 😰 恐惧               │  ← 少女视角线
│ 💭 想回家练琴          │  ← 更关心的事
│ 🔒 忠诚度 30          │
│ ⚠️ 暗杀威胁：高        │
└──────────────────────┘
```

**每张卡片显示的字段**：

| 区域 | 字段                                        | 显示方式                     |
| ---- | ------------------------------------------- | ---------------------------- |
| 身份 | `职位名` (key) + `姓名` + `年龄` + `原身份` | 文字                         |
| 政治 | `对职务理解度` + `实际履职状态`             | 彩色标签                     |
| 操控 | `幕后操控者` + `操控程度` (进度条)          | 进度条 + 文字                |
| 反抗 | `与官僚体系的争斗状态`                      | 标签（仅当非"无争斗"时显示） |
| 心理 | `对当前职位的态度` + `当前心理状态`         | 图标 + 文字                  |
| 关心 | `更关心的事`                                | 斜体小字                     |
| 安全 | `暗杀威胁等级` + `人身安全状况`             | 颜色图标                     |

**交互**：

- 点击卡片 → 展开完整详情（包括 `影响力来源`、`官僚体系的应对`、`被操控方式`）
- 操控程度/忠诚度变化时 → 短暂闪烁动画

#### 移动端适配

手机端内阁面板强制单列卡片，不使用多列网格：

```
┌──────────────────────┐
│ 🏛️ 外务大臣            │
│ 丰川祥子 / 16岁        │
│ 理解度：完全不懂        │
│ 履职：恐慌中            │
│ 操控 █████░░ 85        │
│ 心理：恐惧              │
│ [详情]                 │
└──────────────────────┘
```

- 卡片宽度：`width: 100%`，不设固定宽度
- 长字段（如 `更关心的事`、`被操控方式`）默认折叠，点击「详情」按钮展开
- 手机端使用明确的「详情」按钮而非整卡点击，避免误触
- 详情展开使用底部抽屉而非居中弹窗

#### store.ts 特殊处理

内阁面板需要监听整个 `日本执政.本届内阁.主要阁僚` 对象。由于该对象是 `z.record()`，新增/删除阁僚时 store 会自动感知。

---

### 3.3 政策追踪器（优先级：🟡 中，第3轮施工）

**定位**：以时间线形式展示当前所有活跃政策的生命周期状态。

**布局**：

```
政策追踪                           [+展开全部]
──────────────────────────────────────────────
📋 粮食紧急进口令
   类型：外部势力施压  │  状态：执行中 ⏳
   提出者：事务次官会议  │  主管：农林水产省
   效果评估：适得其反 ⚠️
   副作用：朝总联借机扩大港口控制力
   ▸ 详情
──────────────────────────────────────────────
📋 CSDC结算体系扩大令
   类型：官僚推动  │  状态：审议中 📝
   提出者：经产省事务次官  │  对CSAT影响：+45
   ▸ 详情
──────────────────────────────────────────────
```

**每个政策条目显示的字段**：

| 字段                      | 显示方式                             |
| ------------------------- | ------------------------------------ |
| `内容`                    | 标题                                 |
| `类型`                    | 彩色标签（7色对应7类型）             |
| `推行状态`                | 进度图标（提案中→审议中→执行中→...） |
| `提出者` + `提出者理解度` | 小字（理解度低时红色警告）           |
| `主管省厅` + `主管阁僚`   | 标签                                 |
| `效果评估`                | 颜色图标                             |
| `副作用`                  | 折叠区域（默认隐藏）                 |
| `对内阁稳定度影响`        | +/- 数字标签                         |

**政策推行状态图标映射**：

| 状态         | 图标 | 含义          |
| ------------ | ---- | ------------- |
| 提案中       | 📝    | 起草阶段      |
| 审议中       | 📖    | 议会/官僚审议 |
| 已通过待执行 | ✅    | 已批准        |
| 执行中       | ⏳    | 正在推行      |
| 受阻         | 🚫    | 遭遇阻力      |
| 搁置         | ⏸️    | 无限期暂停    |
| 废除         | ❌    | 已撤销        |
| 已完成       | 🏁    | 政策落地      |

#### 移动端适配

手机端不使用横向时间轴，改为纵向政策列表：

```
📋 粮食紧急进口令
类型：外部势力施压
状态：执行中 ⏳
提出者：事务次官会议
效果：适得其反 ⚠️
[展开副作用]
```

- 每个政策是纵向卡片，宽度 `100%`
- 副作用、影响势力、详细评估默认折叠
- 数字影响用小标签，如 `稳定度 -12`
- 横向并排的「类型 │ 状态」在手机端改为换行显示

---

### 3.4 选举看板（优先级：🟡 中，第3轮施工）

**定位**：根据当前选举模式，显示不同的选举界面。

#### 模式A：正常首相指名

```
🏛️ 首相指名选举                           热度：45%
──────────────────────────────────────────────
执政党推荐：河野太郎（自民党）
在野党联合：枝野幸男（立宪民主党）

议会势力分布：
  自民党 ██████████ 260席  → 支持河野
  立宪   ██████ 150席    → 支持枝野
  公明党 ████ 90席       → 观望
  维新   ██ 50席         → 反对

当前阶段：众议院指名投票中
```

#### 模式B：2039特别大选

```
🌐 2039 特别大选                          热度：78%
青少年参选比例：34%  │  恶搞热度：82%
──────────────────────────────────────────────
候选人排名：
1. 🥇 初音未来（虚拟角色）      1,245,032票  ⚠️ 高威胁
2. 🥈 高咲侑（虹咲学园）          892,410票  ⚠️ 中威胁
3. 🥉 河野太郎（自民党）          456,200票
4.    丰川祥子（Ave Mujica）     234,100票  ⚠️ 极高威胁
5.    桐谷透子（Morfonica）      198,700票

外部干预痕迹：朝总联组织大量账号为亲CSAT候选人投票
当前阶段：互联网初选中
```

**候选人威胁等级颜色**：

| 威胁等级 | 颜色 | 含义         |
| -------- | ---- | ------------ |
| 无       | 灰   | 安全         |
| 低       | 绿   | 基本安全     |
| 中       | 黄   | 有风险       |
| 高       | 红   | 暗杀风险极高 |

#### store.ts 特殊处理

选举看板需要根据 `日本执政.选举进程.类型` 动态切换显示模式。

#### 移动端适配

手机端选举看板拆成单列榜单：

```
🌐 2039 特别大选
热度 78%｜恶搞 82%

1. 初音未来
   1,245,032票｜高威胁

2. 高咲侑
   892,410票｜中威胁
```

- 候选人排名使用单列列表，票数和威胁等级放第二行
- 外部干预痕迹折叠显示
- 议会势力分布的横向进度条改为短进度条 + 数字（如 `自民党 260席`）
- 青少年参选比例、恶搞热度等统计信息用紧凑标签

---

### 3.5 危机仪表盘（优先级：🟢 低，第4轮施工）

**定位**：四维雷达图 + 潜在危机列表，可视化国家崩溃风险。

**布局**：

```
⚠️ 国家危机仪表盘              整体等级：严重 🔴
──────────────────────────────────────────────
        治安
        ▲
      5 │
        │    ╲
  经济  │  ●─── 粮食
        │    ╱
        ▼
       能源

治安：严峻 🔴    粮食：黑市依赖 🔴
经济：恶性通胀 🔴  能源：计划停电 🟡
──────────────────────────────────────────────
潜在危机：
🔴 粮食危机引发大规模社会动荡
🟡 恶性通胀导致经济体系崩溃
🟡 社会治安崩溃引发全面恐慌
──────────────────────────────────────────────
各区域分化：
  东京都心：生命维持餐依赖区 + 黑市繁荣
  沼津：极右翼反CSAT据点（Aqours武装自警团）
  冲绳：怀念美国时代的情绪蔓延
```

**四维雷达的数据来源**：

| 维度 | 数据来源                    | 映射（0-5）                              |
| ---- | --------------------------- | ---------------------------------------- |
| 治安 | `日本国内情势.治安等级`     | 正常=5, 恶化=4, 严峻=3, 失控=2, 无政府=1 |
| 经济 | `日本国内情势.通胀等级`     | 稳定=5, 温和=4, 严重=3, 恶性=2, 崩溃=1   |
| 粮食 | `日本国内情势.粮食供应等级` | 充足=5, 紧缺=4, 危机=3, 黑市=2, 饥荒=1   |
| 能源 | `日本国内情势.能源供应等级` | 正常=5, 计划停电=3, 严重短缺=2, 崩溃=1   |

**潜在危机列表**：直接读取 `事件队列.潜在危机`，按 `严重程度` 排序。

#### 移动端适配

手机端雷达图可读性差（iframe 宽度小），提供替代显示：

- **桌面端**：四维雷达图 + 潜在危机列表
- **手机端**：四个纵向指标卡片（雷达图可折叠或隐藏）

手机端替代布局：

```
治安  严峻 🔴
██████░░░░ 3/5

粮食  黑市依赖 🔴
████░░░░░░ 2/5

经济  恶性通胀 🔴
████░░░░░░ 2/5

能源  计划停电 🟡
██████░░░░ 3/5
```

- 每个维度独立一行：标签 + 等级 + 颜色图标 + 进度条
- 潜在危机列表保持纵向排列不变
- 各区域分化信息默认折叠

---

### 3.6 流式楼层增强（优先级：🟢 低，第4轮施工）

**定位**：增强酒馆原生消息楼层，在 AI 回复中高亮关键变量信息、提供折叠分段。

**功能列表**：

| 功能         | 说明                                                        |
| ------------ | ----------------------------------------------------------- |
| 变量高亮     | 自动识别 `_.set()` 命令，解析并显示为"变量变更摘要"标签     |
| 危机预警标签 | 当 `$整体危机等级` 变化时，在楼层顶部插入预警横幅           |
| 分段折叠     | 将长消息按段落折叠（参考 `示例/流式楼层界面示例/分段.vue`） |
| 阁僚状态气泡 | 当消息提及某阁僚时，在消息旁悬浮显示该阁僚的当前状态卡片    |

**变量高亮示例**：

```
AI回复正文...

📊 [变量更新] 世界.政治阶段 → 特别大选报名期
📊 [变量更新] 选举进程.阶段 → 互联网报名
📊 [变量更新] 舆论场.社交媒体热点 → 追加 #特别大选开始

...AI回复正文继续
```

**实现方式**：使用 `mountStreamingMessages` 挂载，宿主选择 `div`（需继承酒馆样式）。

```ts
// index.ts
import { mountStreamingMessages } from '@util/streaming';
import App from './App.vue';

$(() => {
  const { unmount } = mountStreamingMessages(() => createApp(App), { host: 'div' });
  $(window).on('pagehide', () => unmount());
});
```

---

### 3.7 悬浮窗口（优先级：🟢 低，第5轮施工）

**定位**：脱离消息楼层的全局悬浮窗口，用于处理衍生内容——阁僚详情弹窗、政策详情抽屉、事件通知等主面板中放不下的次要信息。

**实现方式**：**脚本**项目（仅有 `index.ts`，没有 `index.html`），用 jQuery 在酒馆 DOM 中注入 `position: fixed` 定位元素，通过 `Mvu.getMvuData()` 或 `getVariables()` 读取数据。

**为什么不用 iframe**：

| 方式                          | 说明                        | 问题                                               |
| ----------------------------- | --------------------------- | -------------------------------------------------- |
| ❌ iframe 内 `position: fixed` | iframe 嵌入在消息楼层容器中 | `fixed` 受限于 iframe 边界，无法溢出到整个酒馆页面 |
| ❌ 多 iframe 间 `postMessage`  | 主面板与悬浮 iframe 通信    | 架构复杂，延迟高                                   |
| ✅ **脚本 + jQuery**           | 脚本直接操作酒馆 DOM        | 简单直接，数据同源                                 |

**适用场景**：

- **阁僚详情卡片**——点击主面板中的阁僚 → 悬浮窗口显示完整信息（影响力来源、官僚体系应对、被操控方式等）
- **政策详情抽屉**——展示政策的完整副作用链条和对各势力的影响
- **事件通知横幅**——危机升级时在页面顶部弹出警示横幅
- **迷你状态悬浮球**——酒馆页面角落常驻简化的状态指示器
- **选举实时弹幕**——选举期间的舆论动态滚动

**伪代码示例**：

```ts
// src/2039_悬浮窗口/index.ts
import FloatingWindow from './FloatingWindow.vue';

$(() => {
  // 在酒馆页面中注入一个 fixed 定位的容器
  const container = $('<div id="th2039-floating-window"></div>').appendTo('body');

  // 挂载 Vue 应用（mount 到 body 内的 DOM 元素，而非 iframe）
  const app = createApp(FloatingWindow);
  app.mount(container[0]);

  $(window).on('pagehide', () => {
    app.unmount();
    container.remove();
  });
});
```

**关键技术点**：

- 脚本注入的元素使用 `position: fixed; z-index: 9999;` 脱离正常文档流
- 数据通过 `Mvu.getMvuData()` 直接读取（不经过 `defineMvuDataStore`）
- 如需响应式更新，可在脚本内建立简单的 `setInterval` 轮询，或监听 MVU 事件 `Mvu.events.VARIABLE_UPDATE_ENDED`
- 脚本内也可以使用 Vue + Pinia，但需手动挂载到 `body` 而非 iframe 内
- 注意酒馆主题切换时悬浮窗口的颜色适配

#### 移动端适配

手机端悬浮窗口容易遮挡输入框、按钮和消息内容，需要特殊处理：

| 桌面端         | 手机端                             |
| -------------- | ---------------------------------- |
| 右下角悬浮窗口 | 右下角悬浮球（默认收起）           |
| 可展开侧边面板 | 点击后底部抽屉                     |
| 可显示多条通知 | 通知自动合并，只显示最高危机       |
| 可拖动         | 不建议默认拖动，避免和页面滚动冲突 |

手机端默认只显示一个小悬浮球：

```
● 2039
```

点击后展开为底部抽屉，而不是右侧面板。

**关键规则**：

- 悬浮球位置：右下角偏上，避免遮挡酒馆输入框
- `bottom` 不要固定死，应考虑输入区高度
- 展开时覆盖半屏，而不是全屏常驻
- 底部抽屉留出安全区：`padding-bottom: env(safe-area-inset-bottom)`
- 关闭按钮明显，点击遮罩也可关闭

---

## 四、样式指南

### 4.1 配色体系

| 用途          | 颜色      | Tailwind                    |
| ------------- | --------- | --------------------------- |
| 危险/极高     | `#ef4444` | `text-red-500` `bg-red-500` |
| 警告/高       | `#f97316` | `text-orange-500`           |
| 注意/中       | `#eab308` | `text-yellow-500`           |
| 正常/低       | `#22c55e` | `text-green-500`            |
| 信息/进行中   | `#3b82f6` | `text-blue-500`             |
| 特殊/关键节点 | `#8b5cf6` | `text-purple-500`           |
| 稳定          | `#06b6d4` | `text-cyan-500`             |

### 4.2 组件尺寸约束

- 状态栏：最大宽度 100%，高度自适应（约 60-80px）
- 卡片（阁僚/政策）：约 200-280px 宽，高度自适应
- 仪表盘面板：约 350-400px 宽
- 所有面板：禁止使用 `vh` 单位，使用 `aspect-ratio` 控制高度
- 不要有横向滚动条
- 手机端卡片强制 `width: 100%`，不使用固定宽度

### 4.3 动画

- 数值变化：CSS `transition: all 0.3s ease`
- 危机等级升级：进度条红色脉冲动画（`@keyframes pulse-red`）
- 新事件发生：卡片短暂闪烁（黄色边框 1s 后消失）
- 阶段切换：标签 0.3s 渐变切换

### 4.4 手机端适配

- 以 320px 宽度作为最低适配目标，所有核心信息必须在 360px 宽度下无横向滚动
- 状态栏默认只显示三项核心信息（政治阶段、首相、危机），其他信息折叠
- Tab 栏允许横向滚动，按钮触摸区域不小于 `44px`
- 卡片在手机端强制单列，不使用多列网格
- 详情弹窗在手机端改为底部抽屉（`position: fixed; bottom: 0`），留出安全区 `env(safe-area-inset-bottom)`
- 悬浮窗口在手机端默认收起为悬浮球，点击展开为底部抽屉
- 禁止依赖 `hover` 作为关键交互；所有交互必须支持点击/触摸
- 避免固定宽度，优先使用 `width: 100%`、`max-width`、`clamp()`、`minmax()`
- 不使用 `vh` 作为主高度单位，避免被移动端浏览器地址栏影响
- 使用 CSS Container Query（`container-type: inline-size`）根据容器宽度切换布局，而非依赖 `window.innerWidth`（因为主面板在 iframe 内）
- 激活态必须靠颜色 + 边框 + 字重区分，不只靠颜色（照顾色觉障碍用户）

---

## 五、施工阶段

### 阶段一：主面板骨架（第1轮）

**目标**：搭一个最小可用的主面板——状态栏 + Tab 容器，验证 store 绑定和变量同步。

**文件**：

```
src/2039_ui/主面板/
├── index.html          ← <div id="app">
├── index.ts            ← createApp(App).mount('#app')
├── store.ts            ← defineMvuDataStore(Schema, ...)
├── App.vue             ← 容器：状态栏 + Tab 切换
└── 状态栏.vue           ← 状态栏主组件
```

**App.vue 最小实现**：

- 状态栏一行显示：政治阶段标签 + 首相状态 + 危机等级
- Tab 栏显示4个标签，点击可切换/折叠
- 数据全部来自 `useDataStore().data`
- Tab 内容区先用占位文本，后续填充

### 阶段二：内阁面板（第2轮）

**目标**：实现主面板的「内阁」Tab，展示所有阁僚的卡片网格。

**文件**：

```
src/2039_ui/主面板/
├── ...（已有文件）
├── 内阁面板.vue         ← Tab: 阁僚卡片网格
└── 阁僚卡片.vue         ← 单张卡片组件
```

**关键实现**：

- `v-for` 遍历 `data.日本执政?.本届内阁?.主要阁僚`
- 每张卡片分"政治线"和"少女线"两个区域
- 操控程度进度条、暗杀威胁等级颜色标记
- 点击卡片 → 通过 `详情弹窗.vue` 展示完整信息

### 阶段三：政策追踪 + 选举看板（第3轮）

**目标**：实现主面板的「政策」「选举」Tab。

**文件**：

```
src/2039_ui/主面板/
├── ...（已有文件）
├── 政策追踪.vue         ← Tab: 政策时间线
├── 政策条目.vue         ← 单条政策组件
├── 选举看板.vue         ← Tab: 根据模式切换显示
└── 详情弹窗.vue         ← 通用详情弹窗/抽屉组件
```

**关键实现**：

- 政策追踪：时间线形式展示活跃政策生命周期
- 选举看板：根据 `日本执政.选举进程.类型` 动态切换正常选举 / 特别大选模式
- 所有组件共享同一 store

### 阶段四：危机仪表盘 + 流式增强（第4轮）

**目标**：实现主面板的「危机」Tab + 独立的流式楼层增强。

**文件**：

```
src/2039_ui/主面板/
├── ...（已有文件）
├── 危机仪表盘.vue       ← Tab: 四维雷达 + 潜在危机

src/2039_ui/流式增强/
├── index.ts            ← mountStreamingMessages
├── store.ts
└── App.vue             ← 变量高亮 + 分段折叠
```

**关键实现**：

- 四维雷达图（治安/经济/粮食/能源）使用 SVG 或 CSS 绘制
- 潜在危机列表按严重程度排序
- 流式增强为独立脚本项目，不依赖主面板

### 阶段五：悬浮窗口（第5轮，后期）

**目标**：用脚本实现脱离消息楼层的全局悬浮窗口，处理衍生内容。

**文件**：

```
src/2039_悬浮窗口/
├── index.ts            ← 脚本入口，注入 fixed 元素 + 挂载 Vue
├── FloatingWindow.vue  ← 悬浮窗口主组件
└── ...（按需添加子组件）
```

**关键实现**：

- 脚本在酒馆 `body` 中注入 `position: fixed` 容器
- 通过 `Mvu.getMvuData()` 读取数据，自行轮询或监听 MVU 事件
- 可响应主面板中触发的交互（如点击阁僚 → 悬浮窗口展示详情）

---

## 六、关键实现细节

### 6.1 `z.record()` 的 v-for 遍历

由于 `z.record()` 解析后的对象 **不保证** 是普通对象（可能包含原型链属性），遍历时必须过滤：

```vue
<template>
  <div v-for="[职位, 阁僚] in 阁僚条目" :key="职位">
    <!-- 阁僚卡片 -->
  </div>
</template>

<script setup lang="ts">
import { useDataStore } from './store';
const { data } = storeToRefs(useDataStore());

const 阁僚条目 = computed(() => {
  const 阁僚们 = data.value.日本执政?.本届内阁?.主要阁僚 ?? {};
  // _.entries() 只返回 own enumerable properties
  return _.entries(阁僚们) as [string, typeof 阁僚们[string]][];
});
</script>
```

### 6.2 派生字段的访问

派生字段（`$执政合法性` 等）在 `deriveAll` 中通过 `...data` 展开后附加。在 Vue 模板中直接访问：

```vue
<template>
  <span>{{ data.$整体危机等级 }}</span>
</template>
```

但 TypeScript 类型可能不包含 `$` 字段（因为 `Schema.transform()` 的输出类型与输入不同）。解决方案：在 store.ts 中使用时做类型断言，或定义扩展类型。

### 6.3 性能注意事项

- `useDataStore().data` 是深度响应式（`deep: true`），每2秒全量替换
- 不要在 `computed` 中进行重型计算，尽量直接读取
- 大列表（如所有角色情况）考虑虚拟滚动（但2039角色数通常<20，不需要）
- SVG 雷达图避免在 `computed` 中重新生成 path，使用 `watch` + 缓存

### 6.4 空值处理

所有字段都可能为 `undefined`（初始化前/数据不完整时），模板中必须做好兜底：

```vue
<template>
  <span>{{ data.日本执政?.首相姓名 || '空位' }}</span>
  <ProgressBar :value="data.日本执政?.本届内阁?.内阁稳定度 ?? 0" />
</template>
```

### 6.5 容器查询与移动端布局切换

由于主面板在 iframe 内，`window.innerWidth` 反映的是 iframe 自身的宽度而非酒馆页面宽度。因此移动端布局切换应使用 **CSS Container Query** 而非 Media Query：

```scss
// App.vue
.main-panel {
  container-type: inline-size;
  container-name: main-panel;
}

// 状态栏.vue
@container main-panel (max-width: 430px) {
  .status-bar {
    font-size: 12px;
  }
  .status-row-2 {
    display: none; // 手机端默认折叠第二行
  }
  .status-row-2.expanded {
    display: flex;
  }
}

// 内阁面板.vue
@container main-panel (max-width: 430px) {
  .cabinet-grid {
    grid-template-columns: 1fr; // 手机端单列
  }
}

// 危机仪表盘.vue
@container main-panel (max-width: 430px) {
  .radar-chart {
    display: none; // 手机端隐藏雷达图
  }
  .crisis-cards {
    display: block; // 显示替代的纵向指标卡片
  }
}
```

**断点定义**：

| 断点名称 | 容器宽度      | 布局策略             |
| -------- | ------------- | -------------------- |
| `narrow` | ≤ 430px       | 单列、折叠、底部抽屉 |
| `medium` | 431px ~ 768px | 单列但信息更完整     |
| `wide`   | ≥ 769px       | 多列网格、居中弹窗   |

**详情弹窗的移动端适配**：

| 设备               | 详情组件形态        |
| ------------------ | ------------------- |
| 桌面端（`wide`）   | 居中弹窗 / 右侧抽屉 |
| 平板端（`medium`） | 右侧抽屉            |
| 手机端（`narrow`） | 底部抽屉，覆盖半屏  |
| 极窄屏（≤ 320px）  | 全屏详情页          |

---

## 七、附录

### A. 颜色常量（TypeScript）

```ts
// 可在各界面共享的颜色映射
export const 政治阶段颜色: Record<string, string> = {
  '遇刺潮末期': '#ef4444',
  '看守停摆期': '#f97316',
  '正常选举期': '#eab308',
  '特别大选报名期': '#22c55e',
  '特别大选初选期': '#3b82f6',
  '特别大选指名期': '#8b5cf6',
  '新内阁执政期': '#06b6d4',
};

export const 危机等级颜色: Record<string, string> = {
  '正常': '#22c55e',
  '注意': '#eab308',
  '警戒': '#f97316',
  '严重': '#ef4444',
  '危急': '#7f1d1d',
};
```

### B. 图标速查（FontAwesome）

| 用途     | 图标类名                           |
| -------- | ---------------------------------- |
| 首相     | `fa-solid fa-crown`                |
| 内阁     | `fa-solid fa-landmark`             |
| 选举     | `fa-solid fa-check-to-slot`        |
| 危机     | `fa-solid fa-triangle-exclamation` |
| 政策     | `fa-solid fa-file-lines`           |
| 经济     | `fa-solid fa-chart-line`           |
| 治安     | `fa-solid fa-shield-halved`        |
| 粮食     | `fa-solid fa-wheat-awn`            |
| 能源     | `fa-solid fa-bolt`                 |
| 外部压力 | `fa-solid fa-globe`                |
| 阁僚     | `fa-solid fa-user-tie`             |
| 暗杀威胁 | `fa-solid fa-skull`                |
| 忠诚度   | `fa-solid fa-heart`                |

### C. 与变量施工指南的关系

| 变量施工指南                 | 前端施工指南                       |
| ---------------------------- | ---------------------------------- |
| 定义数据结构（Schema）       | 显示数据结构                       |
| `mvu_schema.ts`              | `主面板/store.ts` 导入 Schema      |
| `index.ts` 注册 Schema       | `主面板/index.ts` 创建 Vue 应用    |
| `constants.ts` 枚举值        | 颜色映射、图标映射                 |
| `transforms.ts` 派生计算     | `$` 前缀字段直接显示               |
| `VARIABLE_UPDATE_ENDED` 联动 | 主面板 2 秒轮询 + 悬浮窗口事件监听 |
